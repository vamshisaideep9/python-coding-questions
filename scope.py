# Scope defines the visibility and accessibility of variables within different parts of a python program.

"""

1. Local Scope: Variable declared inside the function.

2. Global scope: Variable declared outside the function.

3. Nonlocal scope: variable inside an outer function can be 
accessed by an inner function using the nonlocal Keyword.

4. Built-in scope: Built-in functions like print(), len(), max()


"""