"""
Python uses automatic memory management with features like

1. Reference counting
-> Keeps track of number if references to an object using reference counting.
when the reference count reaches zero, the object is deleted.

2. Garbade collection
-> To remove objects not accessible due to circular references.

3. Memory pool management
- Uses memory pools to optmize allocation (small objects)



"""